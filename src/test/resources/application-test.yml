spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:test}
    password: ${DB_PASSWORD:test}
    data: classpath:data-test.sql
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/admin
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs
  sql:
    init:
      schema-locations: classpath:schema-test.sql
      data-locations: classpath:data-test.sql
      mode: always

keycloak:
  auth-server-url: http://localhost:8080
  realm: mac-apps
  resource: tech-hive-store-api
  principle-attribute: preferred_username
  credentials:
    secret: ENC(4Wb9DlLL3XGCO5Jvx03E4RN6jJryNjO1vlD+YP4y7oz5km3Wc3+6tDnjNL7HjS8kr3btqyc9JoggojdZwVvcawBp9rJo4DK3jFSHrh1Socg=)
  admin:
    username: admin
    password: admin

jasypt:
  encryptor:
    password: ilovecode

logging:
  level:
    root: info
    org:
      springframework:
        web: debug
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace

testcontainers:
  reuse:
    enable: true

server:
  port: 9500