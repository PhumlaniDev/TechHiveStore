spring:
  application:
    name: Tech Hive e-Store

  datasource:
    driver-class-name:
    url: jdbc:tc:postgresql:15.3:///testdb
    username: test
    password: test

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
    enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/admin
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

keycloak:
  auth-server-url: http://localhost:8080/
  realm: mac-apps
  resource: ecommerce
  credentials:
    secret: ENC(8qdhL2pKBF3zLVptfntQ/yy+0+XWq8Tf8nmlWdHvuRB37VAZd3EiSeDWYZuTTq51xbpGSwhGG6Kjr1F2DfGejSmcvEOiMpmuyR32yvPwgh0=)
  admin:
    username: admin
    password: admin

#  sql:
#    init:
#      mode: always

jwt:
  auth:
    converter:
      resource-id: ecommerce
      principle-attribute: preferred_username

logging:
  level:
    root: info
    org:
      springframework:
        web: debug